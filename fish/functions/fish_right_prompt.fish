function fish_right_prompt --description 'Write out the right prompt'
    #set -l fg (set_color '#875f5f')
    #set -l bg (set_color -o -b '#875f5f')
    #set -l reset (set_color normal)
    ##directory
    #command git -C . rev-parse 2>/dev/null 
    #if test $status -eq 0
    #    set -l branch (command git -C . rev-parse --abbrev-ref HEAD)
    #    set -l dirty (command git -C . status --porcelain)
    #    if test -n "$dirty"
    #        set -l dirty '!'
    #    end
    #    printf "$fg$reset$bg$branch$dirty$reset$fg$reset"
    #end
    

end
